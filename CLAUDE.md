# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

@AGENTS.md

## 現状

Quantis Light（個人用の日本株スクリーナー。仕様は `docs/harness/spec.md`）を、ハーネスでスプリントごとに構築した。Sprint 14（ウォッチリストと新規該当）まで実装済みで、スプリント計画の実施分はすべて完了している。Sprint 15（F14 AI による判定補助。オプション）は、ユーザーの決定により実施しない（`docs/harness/spec.md` は planner の成果物なので、計画の記述はそのまま）。

## 技術スタック

- TypeScript（strict）、Next.js 16（App Router、Turbopack）、React 19
- shadcn/ui（radix ベース）＋ Tailwind CSS v4、lucide-react
- Supabase（Auth ＋ Postgres）。Next.js 連携は `@supabase/ssr`（Cookie セッション）
- Vitest（単体テスト）、Playwright（E2E）
- pnpm。Supabase CLI は devDependency（`pnpm supabase ...`）。Docker が必要

Next.js 16 は学習データと異なる点が多い（middleware は `src/proxy.ts` に改名など）。実装前に `node_modules/next/dist/docs/` を確認すること。

## コマンド

| コマンド | 内容 |
|---|---|
| `pnpm db:start` / `pnpm db:stop` | ローカル Supabase の起動／停止（API 54321、DB 54322、Studio 54323、Mailpit 54324） |
| `pnpm db:reset` | DB を作り直してマイグレーションを適用（ユーザーも消える） |
| `pnpm env:local` | `supabase status` から `.env.local` を生成 |
| `pnpm seed:users` | ローカル専用。評価用ユーザー（owner・owner2＝許可、intruder＝許可リスト外。パスワードは `scripts/seed-users.ts`）を作成（冪等）。owner2 は手動補正がユーザーごとであることの確認用 |
| `pnpm auth:add-user --email <e> --password <p> [--reset-password]` | 許可リストへの追加とユーザー作成 |
| `pnpm dev` / `pnpm build` / `pnpm start` | http://localhost:3000（別のポートは `pnpm dev -p 3100`。E2E は `E2E_PORT=3100 pnpm test:e2e`） |
| `pnpm lint` / `pnpm typecheck` | ESLint / `tsc --noEmit` |
| `pnpm test` | Vitest（`src/**/*.test.ts`。`*.db.test.ts` を除く）。1ファイルだけ: `pnpm test src/lib/auth/next-path.test.ts` |
| `pnpm test:db` | DB 込みの結合テスト（`src/**/*.db.test.ts`、`vitest.db.config.mts`）。ローカル Supabase と `.env.local` が必要。外部 API だけを差し替えて、取り込み処理を実際の DB に対して動かす。作った行は、テストのファイルごとの接頭辞（9999x・9Y001〜9Y004・9Z001〜9Z120・9W001〜9W008・9W8xx・9W9xx・9V001〜9V010・9V8xx・9V9xx・9U001〜9U014・9U8xx・9T…・9N8xx・9N9xx、書類ID S8TEST・S8SEL・S8DB・S9TEST・S9SEL・S9DB・S9PERF・SXTEST・SXSEL・SXPERF・SYTEST・SYDB・SYPERF・S12NDB、提出者 E999xx・E99Vxx・E9SELxx・E9DBxx・E99Uxx・E9SXxx、性能テストの P0000〜P3999・0000Z〜3999Z・R0000〜R3999・Q0000〜Q3999・T0000〜T3999・N0000〜N3999、実行の details の `fixture` が `sprint-12-db`、プリセットの名前の接頭辞「〔DB13〕」とユーザー `@quantis-db13.local`、Sprint 14 の銘柄コード 9S5xx・9S8xx・9S9xx・性能 S0000〜S3999（社名 DB14…）・実行の details の `fixture` が `sprint-14-db`・テストが作った比較の基準の記録・ユーザー `@quantis-db14.local`）だけを消して後片付けされる。**比較の基準の記録を作るテスト（`screening/screening-changes.db.test.ts`）は、記録（`screening_snapshots`）が0件の DB が前提**（記録の関数は最新 7 回を残して古い記録を消し、比較は全体の最新の記録を使うため。冒頭で確かめて、そうでなければ前提の失敗として落ちる。残っていれば `e2e/fixtures/screening-snapshots-cleanup.sql`）。成功したテストの `console.log` も出す（`vitest.db.config.mts` の `reporters: ["verbose"]`。性能のテストの実測値を残すため。Sprint 11 評価の m5）。株価・財務・有報の結合テストは、銘柄マスタ・EDINET の書類にテスト以外の行が無く、株価の成功の実行・取得済みの開示日が無い DB（`db:reset` 直後）が前提 |
| `pnpm test:e2e` | Playwright（`e2e/`）。ローカル Supabase 起動・`seed:users` 済みで、市場データと実行履歴、評価用ユーザーの条件プリセット・ウォッチリスト、比較の基準の記録が0件の DB が前提（`db:reset` 直後。テストが投入した行は後片付けされる。プリセットが残ったら `e2e/fixtures/screening-presets-cleanup.sql`、ウォッチリストは `watchlist-cleanup.sql`、記録は `screening-snapshots-cleanup.sql`。`/screening`・`/stocks/` を開く spec の `beforeAll` が `expectNoPresets()`・`expectNoSnapshotsOrWatchlist()` で確かめる。`/api/cron/daily` を呼ぶ spec は後片付けで記録も消す）。dev サーバーは未起動なら自動起動（`CRON_SECRET` に E2E 用の値を渡す）。J-Quants のキーが未設定のサーバーが前提（設定済みならキー未設定前提のテストはスキップ）。**リポジトリ直下の `.env`（git 管理外）に有効な `JQUANTS_API_KEY` がある環境では、Next.js がそれを読むので、キー未設定の確認はサーバーを `JQUANTS_API_KEY= pnpm dev -p 3100` のように空の値で起動する**（既にある環境変数は `.env` で上書きされない）。3000 番がほかのアプリで使われているときは `E2E_PORT=3100`、既に起動したサーバーを使うときはその `CRON_SECRET` を `E2E_CRON_SECRET` で渡す。E2E を全件流した後の dev サーバーは、メモリの閾値で自動的に再起動することがある（`approaching the used memory threshold, restarting`。その間の要求が1回だけ遅れる。Sprint 10・11 の評価で観測）。**全件の E2E は本番相当のサーバーで流すのを標準にする**: `pnpm build && JQUANTS_API_KEY= EDINET_API_KEY= CRON_SECRET=local-cron-secret-0123456789 pnpm start -p 3100` を起動してから `E2E_PORT=3100 E2E_CRON_SECRET=local-cron-secret-0123456789 pnpm test:e2e`。dev でしか意味の無い検査（`simulateServerClockBehind()` を使うもの。例 `e2e/stock-detail.spec.ts`・`e2e/ownership-override.spec.ts`・`e2e/ingestion-reliability.spec.ts`）は、`pnpm dev -p 3100` で対象のファイルだけを流す。固定日時の投入例（`screening-example.sql` など。株価の成功の終了が 2026-09-24 20:03 JST）は、その 48 時間後から鮮度の警告のある画面になる（正しい振る舞い。Sprint 12）。**境の後の状態を行を増やさずに再現する方法**（Sprint 12 評価の改善提案）: psql で `data_freshness` の既定の基準時刻だけを一時的に進める（`pg_get_functiondef('public.data_freshness(timestamptz)'::regprocedure)` の `p_now timestamp with time zone DEFAULT now()` を `DEFAULT (now() + '38:00:00'::interval)` に変えて `create or replace` し直す）→ 全件を流す → 元に戻す（同じ定義で `DEFAULT now()` に戻すか、`pnpm db:reset && pnpm seed:users`）。この状態では、時計に依存する `ingestion-reliability.spec.ts` の「47:59 なら出ない」テスト（と同じ describe の残り）が落ちるのが正しい。ほかの spec が落ちなければよい |

初回セットアップ: `pnpm install && pnpm db:start && pnpm db:reset && pnpm env:local && pnpm seed:users && pnpm dev`

## アーキテクチャ

- **ディレクトリ**: `src/app`（ルート）、`src/lib`（ロジック。`auth/`、`supabase/`、`http/`、`dashboard/`、`ingestion/`、`listing/`、`financials/`、`screening/`、`stocks/`、`ownership/`、`watchlist/`、`text/`（入力の空白の文字集合など））、`src/components`（`ui/` は shadcn 生成物。`shell/`・`theme/`・`dashboard/`・`imports/`・`screening/`・`stocks/`・`financials/`・`ownership/`・`watchlist/` は画面の部品）、`supabase/migrations`（DB スキーマの正本）、`scripts/`（運用コマンド）、`e2e/`（`e2e/fixtures/` は契約の投入例の SQL。E2E と `pnpm test:db` が共有する）
- **認証・アクセス制御（多層）**
  1. Supabase Auth: `config.toml` で新規登録・匿名サインインを無効化。Custom Access Token Hook（`private.custom_access_token_hook`）が許可リスト（`private.allowed_emails`）外へのトークン発行を拒否
  2. `src/proxy.ts`: セッション更新と楽観的チェック（未ログインの画面は `/login?next=`、`/api/*` は 401）
  3. 保護画面: `src/app/(app)/layout.tsx` → `requireAllowedUser()`（`lib/auth/guard.ts`）が毎リクエスト `getUser` ＋ `current_user_is_allowed()` で検証。許可取り消し・無効な Cookie は `/auth/signout`（Cookie を消せる Route Handler。GET は状態を判定してから破棄）へ、Auth 障害時はセッションを残して `/login` へ
  - ログアウト（`POST /auth/signout`、同一オリジンのみ）とセッション破棄の応答は `Clear-Site-Data: "cache"` を返す。`next dev` は画面を `no-cache` で返すため、これが無いと「戻る」で保護画面がキャッシュから表示される
  4. API: 各 Route Handler の先頭で `requireApiUser()`（`lib/auth/api.ts`）。401／403／503（Auth 障害）
  5. DB: 市場データのテーブルは RLS 有効、anon に権限なし、`authenticated` かつ許可リスト登録済みのみ select。書き込みは service_role のみ。`alter default privileges` で新しいテーブル・関数に anon/authenticated の権限は自動で付かない（必要な権限はテーブル・関数ごとに明示的に grant する）。`e2e/db-privileges.spec.ts` が全テーブル・関数の権限を検査する
  - Auth のメール送信は Send Email Hook（`private.block_auth_email_hook`）ですべて拒否（アプリはメールを使わない）
- **DB のテーブル・関数は必ずマイグレーション（`supabase/migrations`）で作成する。** Studio や `supabase_admin` で直接作らない（`supabase_admin` が作るオブジェクトには、anon／authenticated への既定の権限付与が残っている）。追加後は `e2e/db-privileges.spec.ts` の許可リスト（authenticated が実行できる関数・参照できるテーブル）も更新する
- **Supabase MCP**（`.mcp.json` の `supabase`）: ローカルの Supabase CLI が公開する `http://localhost:54321/mcp`（`pnpm db:start` 中だけ使える。認証なし・ローカル DB のみ）。表・データ・ログ・advisors の確認に使う。**スキーマの変更に `apply_migration`・`execute_sql` を使わない**（上の規則のとおり、ファイルとして `supabase/migrations` に書いて `pnpm db:reset` で適用する。MCP の `apply_migration` はリポジトリにファイルを残さない）
- **Vercel MCP**（`.mcp.json` の `vercel`）: Vercel 公式の `https://mcp.vercel.com`（OAuth。初回は `/mcp` から認証）。デプロイ・ビルドログ・実行時ログ・プロジェクトの確認に使う。環境変数の値の復号や本番の設定の変更は、ユーザーの指示があるときだけ
- **新しい保護画面**は `src/app/(app)/` 配下に置き、`src/lib/navigation.ts` の `NAV_ITEMS` に1行足す（未実装の画面はナビゲーションに出さない）。**新しい API** は `requireApiUser()` を必ず呼び、`jsonNoStore` で返す。**新しい市場データのテーブル**は `public.stocks` と同じ RLS・権限方針にする
- サービスロール（`SUPABASE_SECRET_KEY`）は `src/lib/supabase/admin.ts`（server-only）からのみ使う。画面のデータ読み出しはユーザーのセッション（RLS 経路）で行う
- 配色は `src/app/globals.css` の `light-dark()` トークンで定義。`<html data-theme="light|dark">` で固定でき、未指定なら OS 設定に従う。利用者の選択は Cookie `theme`（`light`／`dark`／`system`）に保存し、ルートレイアウトがサーバー側で `data-theme` を出力する（`src/lib/theme.ts`、`components/theme/`）。淡い背景の上の文字は `*-strong` トークンを使う（WCAG AA 4.5:1）
- **404**: 一致しない URL は `src/app/global-not-found.tsx`（`next.config.ts` の `experimental.globalNotFound`）が、何も投げずに 404 を描画する。ルートレイアウトを通らないので、`<html>`・テーマ・フッターは `src/app/app-document.tsx` を共有し、認証（`requireAllowedUser()`）もこのファイルで行う。global-not-found の中では next/link のクライアント遷移が効かない（URL だけ変わる）ため、アプリ内のリンクは `AppLink`（`components/shell/app-link.tsx`）を使う。global-not-found の中では通常の `<a>` になる。404 の画面では、ナビゲーションのどの項目も現在の画面として示さない（`useIsNotFoundDocument()`。`/imports/zzz` のような配下のパスでも強調しない）
  - **dev の既知の落とし穴（Sprint 1 の B2'、Sprint 2 評価の B1）**: React 19.2 の開発用のパフォーマンス計測は、サーバーコンポーネントの時刻を「サーバーの timeOrigin − ブラウザの timeOrigin」で換算する。**エラーになったコンポーネント**（`notFound()`／`redirect()` や例外を投げたもの）の `performance.measure` だけ、終了時刻が負になる場合のガードが無い。そのため、サーバーの時計がブラウザより遅れていると `'<名前>' cannot have a negative time stamp` が出る。起動直後の dev サーバーでは出ない。長く動かしたサーバーでの発生は評価者が観測している。原因の仮説は、Node の単調時計と壁時計のずれ（実測で 24 分に約 -8ms）。ただし、ジェネレーターの環境で修正前のコードを 28 分動かした試行では、自然には再現しなかった。
    - 対策1（404）: 描画中に `notFound()` を投げない（上記の global-not-found）。
    - 対策2（すべての throw）: `src/instrumentation-client.ts` が、開発時だけ `performance.measure` に渡る負の時刻を 0 に丸める（`src/lib/dev/measure-guard.ts`。React が開始時刻に対して行っているのと同じ丸め）。`redirect()`（許可の取り消し後のクライアント遷移など）や、今後の `notFound()`（銘柄詳細など）も対象になる。本番の React はこの計測をしないので、本番では入れない。
    - 確認方法: E2E の `simulateServerClockBehind()`（`e2e/support.ts`）でブラウザ側の timeOrigin を 60 秒進めると、起動直後のサーバーでも確実に再現する。描画中に throw するページを追加したら、この状態でも確認すること
- **ダッシュボード**: DB 関数 `public.dashboard_summary()`（security invoker、authenticated のみ実行可）が件数と鮮度を1回で返す。画面（`src/app/(app)/page.tsx`）と `GET /api/dashboard` がユーザーのセッションで呼ぶ（`lib/dashboard/summary.ts`）。集計の失敗は 0 件として扱わずエラー表示にする。銘柄0件（上場中と上場廃止の合計）なら空状態。**Sprint 14 から、ダッシュボード（`stockCount`・財務・判定の件数）と取り込み状況の要約（`financial_metrics_summary`・`annual_reports_summary`・`business_results_summary`・株価の初出日の区画）の件数と分母は上場中の銘柄だけで数える**（「保存済みの銘柄数（上場中）」「ほかに上場廃止 N 銘柄」「上場中の N 銘柄のうち」）
- **取り込みの実行履歴**は `public.ingestion_runs`。`financial_metrics`（Sprint 5）と `ownership_judgments`（Sprint 10）はダッシュボードの集計に必要な最小限の列だけで先に作ってあり、各スプリントでマイグレーションにより拡張する
- **取り込み（Sprint 3〜）**: `src/lib/ingestion/`
  - 起動経路は2つ。手動は `POST /api/ingestion/runs`（`requireApiUser()`＋同一オリジンの確認。実行を記録して 202 を返し、本体は `after()` で動かす）、定期実行は `GET /api/cron/daily`（Vercel Cron。`Authorization: Bearer <CRON_SECRET>` だけで認証し、完了まで待つ）。どちらも `runner.ts` の `startIngestionRun()` → `executeIngestionRun()` を呼び、`maxDuration = 300`。
  - `src/proxy.ts` は `/api/cron/` の配下だけを未ログイン判定から外す（`lib/auth/proxy-paths.ts`。`/api/cron`・`/api/cronx` などは外さない）。`/API/...` のような大文字小文字の違うパスも API として 401 にする。
  - 書き込みはサービスロールで、DB 関数（service_role のみ実行可）を通す: `start_ingestion_run`（応答の無くなった実行の後片付け＋開始）、`finish_ingestion_run`（`running` の行だけを更新）、`complete_stock_master_run`（銘柄マスタの upsert と成功の記録を1トランザクション。実行が `running` でなくなっていたら何も保存しない）。読み出し（画面、`GET /api/ingestion`）はユーザーのセッション。
  - 二重実行の防止: `ingestion_runs` の部分一意インデックスで、`running` は全体で1行まで。開始から 15 分以上の `running` は応答なしとみなし、次の開始時に `failed` にする（`STALE_RUN_MINUTES` と SQL の interval を一致させる）。
  - 失敗のメッセージは `errors.ts` の決まった日本語だけ（キーの値や外部 API の応答本文を含めない）。キー未設定なら外部 API を呼ばずに「J-Quants の API キーが設定されていません」で `failed`。
  - 銘柄マスタ: J-Quants API V2 `GET /v2/equities/master`（ヘッダー `x-api-key`）。保存するのは `ProdCat=011`（内国株券）・`Mkt` 0111〜0113・`S33≠9999` の行だけ。対象外の件数は `ingestion_runs.details` に理由ごとに記録する。一覧から消えた銘柄は削除しない（上場廃止の扱いは Sprint 12。`stocks` の行を消すと手動補正が連鎖して消えるので、削除ではなく状態で扱う）。形式の違い（必須項目の欠け、`pagination_key`、コードの重複）は一部だけを保存せずに失敗にする。
  - 設定状態（`config.ts`）は環境変数の有無だけで判定し、画面を開いても外部 API は呼ばない。`CRON_SECRET` は16文字未満なら未設定扱い（画面の表示と Route Handler の認証が同じ `getCronSecret()` を使う）。比較はハッシュ＋`timingSafeEqual`。
  - 取り込み状況の画面（`/imports`）の「今すぐ取り込み」（`components/imports/manual-ingestion.tsx`）は、実行中の間 `router.refresh()` で画面を取り直す。テスト用のフィクスチャは `src/lib/ingestion/jquants/__fixtures__/`（テストからだけ import する）。
  - 新しい取り込み対象を足すときは、`runner.ts` の `SUPPORTED_TARGETS` と `RUNNERS` に加え、`schedule.ts` の `CRON_JOBS` のどれかの `targets` に入れる（`runner.test.ts` が、すべての対象がちょうど1回ずつ定期実行に入っていることを確かめる）。Cron のルートは `lib/ingestion/cron.ts` の `handleCronRequest()` を使う（認証・409・`skipped`・期限を共通にする）。
  - `HEAD /api/cron/*` は明示的に 405（Next.js は HEAD を GET として処理するため。Sprint 3 評価の M2）。
  - J-Quants への1回の要求と応答の分類（210・401・キーの本文の 403・キー以外の 403・429・接続できない・JSON でない）は `jquants/http.ts` の `requestJQuants()` で共通にしている。
- **株価の初出日と推定上場年数（Sprint 4）**: 取り込みは `lib/ingestion/listing-dates.ts`（target `daily_quotes`）、J-Quants の `GET /v2/equities/bars/daily` は `jquants/bars-daily.ts`（`Date`・`Code` だけを検証）
  - 対象は銘柄マスタにあって `stock_listing_dates` に行の無い銘柄だけ（DB 関数 `listing_dates_pending()`）。確定した行は更新しない（保存は `save_stock_listing_dates` の `on conflict do nothing`。AC4.4）。行が無い銘柄だけを処理するので、打ち切られても次の実行で続きから再開する。
  - データ期間の開始日 W: Standard プランの株価は「10年前まで」（移動する期間）。実行日（JST）の10年前の翌日から `date=` で最大14日試し、最初に行のある日を W にする（`listing-period.ts`）。200 の0件・210・400・キー以外の本文の 403 は次の日へ。本文がキーの無効・欠如（`The incoming api key is invalid or expired.`／`The api key is required.`）の 403 と 401 は即失敗（429 は Sprint 12 から待って再試行し、解消しなければ失敗）。試した日ごとの状態は `details.dataStartProbe`。
  - W の日に現れた銘柄は `first_price_date = data_start_date = W`（「データ期間開始以前から上場」）。残りは `code=X&from=W` を全ページ取得して最も古い日付。210・0件は「株価データなし」（行を作らず次回再試行）、500 などはその銘柄だけ失敗。
  - 要求の間隔は 600ms 以上。新しい要求はルートの開始から 210 秒まで（`clock.ts` の `REQUEST_BUDGET_MS`。Cron は銘柄マスタの時間も含めて数える）。超えたら `partial`（残りの銘柄数を文言と `details.remaining` に記録）。保存は 50 銘柄ごとで、そのたびに `processed_count` を足す（`finish_ingestion_run` は `p_processed_count` が NULL なら件数を変えない）。
  - 算出は DB の1か所だけ: `listing_years_between(from, to)`（暦の年。整数部分・正確な値・整数演算での小数1桁の切り上げ）、ビュー `listing_reference_date`（基準日 = `daily_quotes` の成功・一部失敗の最新の終了日時の JST の日付）、ビュー `stock_listing_ages`（security_invoker）。画面と `GET /api/stocks` は DB の値を表示するだけで、アプリ側で年数を計算しない（丸めのずれを作らない）。
  - 表示は `estimated_listing_years`（小数1桁に切り上げた数値。API では JSON の数値なので 3.0 は `3`）。**Sprint 6 の絞り込み・並べ替えは `listing_years_exact`（丸める前）を使う**（閾値が小数1桁までなら「表示 ≤ X ⇔ 正確 ≤ X」）。データ期間開始以前の N は `listing_years_lower_bound`（整数部分。取り込み直後は 9、約2週間で 10）。
  - 画面は `/imports` の区画「株価の初出日と推定上場年数」（`components/imports/listing-dates-panel.tsx`）。1銘柄の確認は Sprint 5 から独立した区画「銘柄コードで確認」（`components/imports/code-lookup.tsx`。`?code=`。フォームは正規化したコードの URL に `router.push` する。描画中に throw しない）で、推定上場年数と財務指標のカードを並べる。`GET /api/stocks?code=` で1銘柄に絞れる（4文字は末尾に 0）。
- **財務データと指標（Sprint 5）**: 取り込みは `lib/ingestion/financials.ts`（target `financials`）、J-Quants は `jquants/fins-summary.ts`（`/v2/fins/summary`）と `jquants/calendar.ts`（`/v2/markets/calendar`）
  - 開示日（`date=`）ごとに全銘柄分を取る。取得範囲は実行日（JST）の6年前〜実行日（`financials-period.ts`）。最初に取引カレンダーを1回取り、`HolDiv` が 1・2 の営業日だけを要求する（取れなければ平日で代用し `details.calendar = "weekdays_fallback"`。キーの無効・401 は打ち切り。429 は Sprint 12 から待って再試行し、解消しなければ打ち切り）。順番は、直近7日の営業日（取得済みでも毎回取り直す。速報→確報）→ 未取得の営業日（新しい順）。
  - 開示日ごとに、全ページを取り終えてから DB 関数 `save_financial_statements`（service_role のみ）で、通期の開示の保存と `financial_fetched_dates` の記録を1トランザクションで行う。取得済みの記録が無い日だけを処理するので、打ち切られても次の実行で続きから再開する。J-Quants 側のデータ全般の修正を取り直すときは `financial_fetched_dates` の行を消す。
  - 要求の間隔は**前の要求の開始から** 1,100ms 以上（`/fins/summary` は 60 回/分の専用枠）。期限は開始から 210 秒（1回で約190回。初回は約8回の実行で取得範囲を埋める）。開示日ごとの失敗（500・210・400・キー以外の 403・形式の違い）は取得済みにせず、5回続いたら打ち切り。
  - 通期実績: `DocType` が `FYFinancialStatements_(Consolidated|NonConsolidated)_(JP|US|IFRS|JMIS|Foreign)`（REIT を除く）かつ `CurPerType = FY` の行だけ。使うのは `Sales`・`OP`（実測: 非連結の短信も値は `Sales`・`OP` に入る。空のときだけ項目ごとに `NCSales`・`NCOP`）。四半期・予想の修正・予想の項目は保存する経路が無い（テーブルの制約でも拒否）。金額は円（実測で確認済み）。値は文字列で、空は `""`。
  - DB は3層（**算出の定義は出典に依存しない**。仕様の改訂2、F15 で EDINET の期が加わる）: ① 出典ごとの保存（決算短信は `financial_statements`。主キーは (code, disclosure_no)。訂正は別の行）→ ② ビュー `financial_periods`（security_invoker。銘柄・事業年度の終了日ごとに1行。出典の優先順位 `source_priority` → 同じ出典の中で開示日・時刻・開示番号の新しい順。`source`・`source_document_id` を持つ。`is_irregular` は 358〜371 日の外）→ ③ 純粋な関数 `financial_metrics_from_periods(jsonb)`（期の配列だけを受け取る。出典の項目は無視）。`recalculate_financial_metrics(codes)` が ② を集めて ③ を呼び、`financial_metrics` に保存する。**`financial_statements` の insert・update・delete（stocks の削除の連鎖を含む）で、文単位のトリガーが同じトランザクションの中で再計算する**（`financial_metrics` は直接書き換えない）。Sprint 9 で EDINET の期（`business_results_periods`）を候補に加えた（下の「上場前の期の補完」）。
  - 売上CAGR: FY0 から連続（新しい期の開始日の前日 = 古い期の終了日）する最大5期で (FY0/FY-4)^(1/4) − 1。理由の優先順位は `irregular_period` → `insufficient_periods`／`non_consecutive_periods` → `revenue_not_disclosed` → `base_revenue_not_positive` → `latest_revenue_negative`。営業利益率は FY0 だけで `operating_profit_not_disclosed` → `revenue_not_disclosed` → `revenue_not_positive`。理由の表示は `lib/financials/display.ts`。連結・単体や会計基準の混在は算出不可にせず `revenue_cagr_mixed_basis`。
  - 丸め: 比率を小数点以下10桁に丸めて保存（`power` の近似で 0.2 が 0.1999… にならないように）。表示は生成列 `*_display_pct`（百分率の小数点以下1桁に**切り捨て**。負の値は小さい方へ）。**Sprint 6 の絞り込み・並べ替えは `revenue_cagr`・`operating_margin`（索引あり）を使う**（「表示 ≥ X ⇔ 保存 ≥ X/100」）。4,000 銘柄での絞り込みは 1ms 未満（`financial-metrics.db.test.ts` の性能のテスト）。
  - 画面は `/imports` の区画「財務指標（売上CAGR・営業利益率）」（`components/imports/financial-metrics-panel.tsx`。要約は DB 関数 `financial_metrics_summary()`（authenticated、security invoker）を1回呼ぶ。取得済みの開示日は最後の財務の実行の `details` から）と、銘柄コードで確認の財務のカード（`financial-card.tsx`）。API は `GET /api/stocks`（指標の列）と `GET /api/financials?code=`（`metrics` と `periods`。無いコードは 404）。
  - 定期実行は `/api/cron/financials`（`0 13 * * *` = 22:00 JST）。株価の初回の取り込みと 210 秒を取り合わないよう、`/api/cron/daily` とは別の関数にし、Hobby の1時間のずれで重ならないよう2時間あけた。
- **スクリーニング（Sprint 6）**: 画面 `/screening`（`src/app/(app)/screening/page.tsx`、部品は `components/screening/`）と `GET /api/screening`。どちらも `lib/screening/queries.ts` から DB 関数 `screen_stocks(jsonb)`（security invoker。RLS が効く）を1回呼ぶ。外部 API は呼ばない（`lib/ingestion` を import しない）
  - **状態はすべて URL**: `cagr`・`margin`・`years`（閾値。十進の文字列のまま DB に渡し numeric で割る）、`off`（`cagr,margin,years`）、`unavailable=include`、`market`（0111/0112/0113）、`sector`（固定の33業種コード。`lib/screening/sectors.ts`）、`sort`・`order`、`page`。検証と正規形は `lib/screening/params.ts`（画面と API で共通。画面は不正な項目だけ既定値にして注記、API は 400 `invalid_params`）。文法は `^-?\d{1,4}(\.\d)?$`＋条件ごとの範囲。同じパラメータの重複は無効、カンマ区切りの重複は除く、未知のパラメータは無視。入力欄だけは NFKC と、U+2212・長音記号などのマイナスの読み替えをする
  - 画面の操作は `ScreeningView`（client）が条件を持ち、`router.replace` で URL を書き換える（履歴を増やさない）。連続した操作は最後の1回だけを書く（数値入力は 400ms、スライダーは 250ms 待つ）。サーバーから古い結果が届いても、後の操作が待っている間は条件を戻さない。URL が外から変わったとき（戻る・進む、最終ページへの置き換え）だけ、サーバーの条件に合わせる
  - **条件ごとの状態**は met／unmet／unavailable／off。オンの条件に unmet があれば除外。「算出不可を含める」でなければ unavailable（財務データなし・理由コードあり・初出日なし・基準日なし）も除外。データ期間開始以前は条件③で unmet（仕様の定義）。金融業は特別扱いしない（ユーザーの決定。銀行は営業利益率が算出不可）
  - **性能（Sprint 4 評価の m4）**: 条件③は `listing_first_date_cutoff(基準日, Z)`（`listing_years_exact <= Z` を満たす最も古い初出日。二分探索で1回だけ）と `first_price_date` の比較。並べ替えも初出日のキー。表示用の年数はページの行（最大100行）だけで `listing_years_between` を呼ぶ。`stock_listing_ages` ビューは使わない。4,000 銘柄で約 20ms（`screening.db.test.ts` が authenticated として 100ms 以内を検査）。並べ替えは値の無い行が常に最後、同じ値はコード順
  - 表示と絞り込みの一致: 「表示 ≥ X ⇔ 保存 ≥ X/100」（財務）、「表示 ≤ Z ⇔ 正確 ≤ Z」（上場年数）を `screening.db.test.ts` が境界の日付まで網羅して確かめる
  - 判定の式（条件ごとの状態と市場・業種の絞り込み）は DB 関数 `screening_evaluate(p_params, p_codes)` の1か所だけ（Sprint 7）。`screen_stocks` と銘柄詳細の `stock_detail` がこれを呼ぶ。判定を変えるときはここだけを直す
  - 条件①の注記は `components/screening/status-mark.tsx` の `CAGR_SUPPLEMENT_NOTE`・`CagrSupplementNote`（`data-testid="cagr-supplement-note"`。スクリーニングと銘柄詳細で共有。フックを使わない部品なので server からも使える）。Sprint 9 で AC6.12 の暫定の注記（`PROVISIONAL_CAGR_NOTE`）を AC15.12 の文言に置き換えた
  - 結果の行は銘柄詳細へのリンク。リンクのクエリと条件の印の title は、表示中の結果の条件（サーバーが判定に使った条件。`ScreeningView` の props の `conditions`・`queryKey`）から作る。入力中の確定前の値は使わない
  - 条件パネルは、市場区分・業種を上に、条件①〜③をその下に置く（1280×800 でスクロールなしに市場区分と AC6.12 の注記が見えるように）
  - テストのコードの接頭辞: E2E と `screening.db.test.ts` は 9999x・9Z001〜9Z120、銘柄詳細（`stock-detail.spec.ts`・`stock-detail.db.test.ts`）は 9999x・9Y001〜9Y004（`e2e/fixtures/stock-detail-example.sql`。`screening-example.sql` の後に入れる）、スクリーニングの性能テストは P0000〜P3999、財務の性能テストは 0000Z〜3999Z。後片付けは自分の接頭辞だけを消す
- **銘柄詳細（Sprint 7）**: 画面 `/stocks/[code]`（`src/app/(app)/stocks/[code]/`、部品は `components/stocks/`）と `GET /api/stocks/[code]`。どちらも `lib/stocks/queries.ts` の `fetchStockPage()` で、DB 関数 `stock_detail(p_code, p_params)`（security invoker。基本情報・初出日と表示用の年数・判定と「結果に含まれるか」）と、既存の `fetchFinancialEntry`（`financial_periods`・`financial_metrics`）をユーザーのセッションで読む。外部 API は呼ばない
  - **クエリはスクリーニングの条件**（`lib/stocks/detail.ts` の `detailConditionsFromParams`。Sprint 6 の `parseScreeningParams` を使う）。条件のパラメータが無ければ既定の条件（`source: default`）。不正な項目は画面では既定値＋注記、API では 400。戻り先（パンくず・「条件を変える」）は正規形のクエリ付きの `/screening?…`
  - 詳細を開いている間だけ、ヘッダーのナビゲーションの「スクリーニング」も同じ戻り先にする（`lib/navigation-href.ts`。`MainNav` が `useSearchParams` で読む）。どの戻り方でも直前の条件と並べ替えが保たれる（AC7.6）。詳細はナビゲーションの項目ではないので `aria-current` は付けない
  - コードは `normalizeStockCode`。正規形と違えば `redirect()`（クエリは保つ）、形が不正・銘柄マスタに無ければ `notFound()` → `stocks/[code]/not-found.tsx`（保護画面の枠の中、HTTP 404。not-found は props を受け取らないので、コードとクエリは client 部品が URL から読む）。タイトルは `generateMetadata`（見つからなければ「銘柄が見つかりません」）。描画中に throw するので、E2E（`stock-detail.spec.ts` の C6）で `simulateServerClockBehind()` の下でも確かめている。`/stocks`・`/stocks/<code>/zzz` はアプリ全体の 404
  - **5期の枠**（`lib/stocks/slots.ts`）: FY0 = 最新の期、FY-k = 1つ新しい枠の期の開始日の前日に終わる期（DB の「連続」と同じ）。1つ新しい枠が「データなし」なら想定の終了日から求める（月末なら前年の同じ月の末日、月末以外は前年の同じ日）。FY0 から連続する枠の数は `revenue_cagr_period_count` と一致する（`stock-detail.db.test.ts`）。枠の外の期は「保存済みの通期実績をすべて表示」
  - グラフ（`components/stocks/financial-chart.tsx`）は HTML と CSS だけ（ライブラリなし。サーバーで描画、ホバーの表示も CSS）。棒の `data-value` は表と同じ百万円の表示値。金額のセルの `title` と `data-yen` は保存値（円）
  - 同じ期に複数の開示がある期は「開示N件（訂正あり）」（`components/financials/period-badges.tsx`。取り込み状況のカードと共有）
  - 取り込み状況の「銘柄コードで確認」から「銘柄詳細を開く」
  - **行から詳細への遷移（Sprint 7 評価の B1）**: 行・コード・社名のリンク（修飾キーなし）は `ScreeningView` の `openDetail` を通る。条件の書き換えを待っている・応答を待っているときは、書き換えをすぐに発行して完了（`isPending` が false）を待ってから詳細へ `router.push` する（後から完了した replace が遷移を上書きしない。履歴のスクリーニングの項目は最後に入力した条件になる）。詳細の URL はクリックした時点で表示中の結果の条件。⌘・中クリックは従来どおり新しいタブ（`e2e/screening-detail-race.spec.ts`）
- **有報（EDINET）の取り込み（Sprint 8）**: target `edinet_reports`（表示「有報」。Sprint 9 から「EDINET」）。取り込みは `lib/ingestion/edinet-reports.ts`、EDINET API は `lib/ingestion/edinet/`
  - 分け方（Sprint 9 の届出書の補完で使い回す）: `edinet/http.ts`（要求と応答の分類）、`edinet/documents-list.ts`（書類一覧 API の検証と行の分類）、`edinet/document-archive.ts`（書類取得 API の ZIP から `XBRL/PublicDoc/*_ixbrl.htm` を取り出す。監査報告書は読まない）、`edinet/xbrl.ts`（**インライン XBRL の汎用の読み取り**。事実を文書の順に、コンテキストは xmlns で名前空間に直して返す。parse5 で HTML として読む）、`edinet/annual-report.ts`（大株主・役員の抽出）、`edinet/decimal.ts`（十進の文字列の桁ずらし）。骨組み `runEdinetPipeline(runId, deps, pipeline)` に「対象の選び方（loadState）」と「書類ごとの処理（process・save・count）」を渡す
  - **EDINET API v2 の実際の振る舞い**（金融庁「EDINET API 仕様書（Version 2）」2026年6月版で確認。キー無しの要求で確かめた点も含む）: キーはクエリの `Subscription-Key`（**URL は秘密**。URL・fetch の例外（`cause`）をログ・例外・`error_message`・`details` に出さない。fetch は `redirect: "manual"` で 3xx は打ち切り）。エラーでも **HTTP 200 で本文にステータス**（401・429 は `{"StatusCode":401,...}`、400・404・500 は `metadata.status`）。書類取得の成功は本文の先頭の ZIP のシグネチャ（`PK\x03\x04`）で判定（不開示の書類は PDF が返る）。分類は `edinet/http.ts` の1か所
  - 流れ: キーの確認（未設定なら外部 API を呼ばずに「EDINET の API キーが設定されていません」で `failed`）→ 銘柄マスタ0件なら失敗 → **書類一覧**（期間 = 実行日（JST）の 450 日前〜実行日。直近7日は毎回・**古い順**、そのほかは未取得の日を新しい順。日付ごとに `save_edinet_document_list` で保存と取得済みの記録を1トランザクション）→ 期限・打ち切り（キーの無効・429/503・3xx・5回連続の失敗）で一覧を取り終えていなければ本文に進まない → **本文**（`edinet_ingestion_state` の `targets` を提出日時の新しい順に取得・抽出し、`save_edinet_extractions`（Sprint 9 で `save_annual_report_extraction` を置き換えた）で1書類ずつ保存。Sprint 9 からは主要な経営指標等も同じ本文で処理し、取得の順は下の「上場前の期の補完」。訂正に区画が無いと分かると元の書類が新たに対象になるので、対象が無くなるまで繰り返す）。要求の間隔は前の要求の開始から 1,000ms、期限は 210 秒
  - `processed_count` = 本文を処理した書類の数（一覧は数えない）。処理済みの書類は取り直さない（`annual_report_extractions` に行があれば処理済み）。取得の失敗（404・500・ZIP でない・接続できない）は処理済みにせず次の実行で再試行（Sprint 12 から失敗の行 `ingestion_run_failures` に残す。`details.failedDocuments` は廃止）。抽出の失敗は理由付きで処理済み。**抽出の規則を直して取り直すときは、その書類の `annual_report_extractions` の行を消す**
  - 保存する書類: 府令 010 の有報（120）・訂正有報（130。証券コード必須）と、届出書（030）・訂正届出書（040。**証券コードが無くても `edinet_code` 付き**。Sprint 9 用。本文はまだ取得しない）。取り下げ: 取下書（`withdrawalStatus` "1"）の親書類と、取り下げられた書類（"2"）を `withdrawn = true`（親が取り下げられたら子も。false に戻さない）。不開示: 開示不開示区分 "1"（開始）・"3"（解除）の操作日時の新しい情報だけを反映（`withheld`、`disclosure_changed_at`）。取り下げ・不開示は使わない・取得しない
  - **使う書類の選び方は DB のビュー1か所**: `annual_report_candidates`（銘柄ごとの対象の事業年度 = `period_end`（訂正は元の書類の値）が最も新しい。**事業年度を決められない書類は使わない**。候補は提出日時の新しい順）→ `annual_report_sections`（大株主・役員の区画ごとに、候補を新しい順に見て、未処理（pending）か `ok`・`invalid_values` の書類で止める。`section_not_found`・`no_xbrl` は飛ばして古い書類へ＝訂正報告書にその区画が無ければ元の有報の記載を使い、画面にそう示す）。詳細は `annual_report_detail(code)`（数値は `::text` の十進の文字列）、取り込み状況は `annual_reports_summary()`（どちらも authenticated・security invoker）
  - 抽出（`edinet/annual-report.ts`。要素名とコンテキストは公開の有報 S100W7OT・S100W4KN・S100W5PD の実データで確認）: 大株主は `jpcrp_cor:NameMajorShareholders`・`AddressMajorShareholders`・`NumberOfSharesHeld`・`ShareholdingRatio` のうち、ID が `CurrentYearInstant` で始まり軸 `MajorShareholdersAxis` のメンバー `No{N}MajorShareholdersMember` だけを持つコンテキストの事実（合計の行・議決権の表などは読まない）。持株比率は**十進の文字列で**百分率にし（`0.0057` → `0.57`）、記載の桁数 `ratio_decimals` = decimals − 2 を保存。表示は「小数点以下は最低2桁、記載がそれより細かければその桁まで、丸めない」（`lib/stocks/annual-report.ts` の `formatRatioPct`）。所有株式数は株。役員は `NameInformationAboutDirectorsAndCorporateAuditors`・`OfficialTitleOrPositionInformationAboutDirectorsAndCorporateAuditors`（`FilingDateInstant`、軸 `DirectorsAndOtherOfficersAxis`、役員ごとの提出者のメンバー）。株主総会の前の提出で「総会後の役員」の表がある書類は、**同じコンテキストで要素名の末尾が `Proposal`** になっている（S100W4KN で確認）ので、それは読まずに `officers_has_post_agm_table` だけを記録する。記載順はインライン XBRL の文書の順（`officers_order_source = inline_document`。表示リンク `_pre.xml` は読んでいない）。役職名は改行を保つ（画面は `whitespace-pre-line`）
  - 定期実行は `/api/cron/edinet`（`0 15 * * *` = 毎日 0:00 JST）。0:00 を過ぎてから動くので実行日（JST）は前日の書類の提出日の翌日になり、直近7日の取り直しに前日が含まれる。EDINET の過去分の一覧は毎日 24 時過ぎに更新される（取り下げられた書類は項目が null になる）
  - **初回の所要**: 一覧 450 日 ≒ 3回、有報 約 3,900 通は1回 100〜150 通で約 26〜39 回（毎日の定期実行だけでは1か月以上）。取り込み状況の画面の「取り込み待ちの書類」と直前の処理件数を見て、手動の「今すぐ取り込み」を続けて押すと早く埋まる。**3月決算の会社が集中する6月末**（数日で約 2,000 通）は、毎日の定期実行だけでは1〜2週間遅れる
  - **キーを設定したときの確認手順**（この環境では EDINET のキーが無く、実 API での取り込みは未確認）: 1) Vercel／`.env` に `EDINET_API_KEY` を設定し、取り込み状況で「設定済み」を確かめる 2) 手動で「有報（EDINET）」を1回実行し、実行履歴の結果と `ingestion_runs.details`（`listDatesFetched`・`documentsListed`・`stoppedReason`・`lastFailedStatus`・`rateLimit`・`discardedFacts`）と実行の詳細（`/imports/runs/<id>`。失敗した書類）を見る 3) 何回か続けて実行し、「有報を取得できた銘柄」が増えること、2回目以降に新しい書類が無ければ処理件数が 0 になることを確かめる 4) 1銘柄の詳細の大株主・役員を EDINET の閲覧ページ（リンク）と見比べる。応答の形が違えば「EDINET の応答の形式が想定と異なります」、書類ごとの失敗は実行の詳細の「失敗した対象」に残る
  - 既知の制限: 取下げは取下書の出た日（直近7日の取り直し）か、日次更新後の元の日付の一覧でしか反映されない。一覧の期間より前の元の有報を持つ訂正は使わない。インラインでない XBRL だけの書類は `no_xbrl`（`no_inline_xbrl`）
  - テストの接頭辞: E2E・`annual-reports.db.test.ts` は 9W001〜9W008・S8TEST…（投入例 `e2e/fixtures/annual-report-example.sql`、後片付け `annual-report-cleanup.sql`）と 9W8xx・S8SEL…、取り込みの結合テストは 9W9xx・S8DB…・`list_date < 2002-01-01`（時計を 2001 年にする）。フィクスチャ（`edinet/__fixtures__/`）は公開の有報の抜粋（S100W7OT・S100W4KN・S100W5PD の大株主・役員のテキストブロック。略歴と見た目の属性は省略）と、同じ形で組み立てる `synthetic.ts`
- **上場前の期の補完（Sprint 9。F15）**: EDINET の有報・届出書の「主要な経営指標等の推移」から各期の売上高・営業利益を読み、決算短信に無い期を補う。条件①の定義は変えない
  - **取り込みは Sprint 8 と同じ target `edinet_reports`**（1回の ZIP の取得で2つの処理。`edinet-reports.ts` の `edinetDocumentsPipeline`）。本文を取得するのは、大株主・役員（`annual_report_extractions`）か主要な経営指標等（`business_results_extractions`）のどちらかが未処理の書類だけ。未処理の処理だけを行い、`save_edinet_extractions` で1トランザクションに保存（処理件数は書類ごとに1）。表示名は、実行履歴・ダッシュボード・実行中の表示が「EDINET」、手動の対象・定期実行が「EDINET（有報・届出書）」（`lib/ingestion/runs.ts` の `RUN_TARGET_LABELS`・`RUN_TARGET_LONG_LABELS` の1か所）
  - 読む書類（ビュー `business_results_targets`）: 有報は Sprint 8 の候補の列（直近の事業年度の書類。元の有報を含む）のすべて、届出書（030・040）は銘柄に結び付くものすべて（取り下げ・不開示を除く）。直近の有報1通に過去5期があるので、上場から1年以上の銘柄は有報だけで補える。取得の順は、条件①を算出できていない銘柄（指標なし・`insufficient_periods`・`non_consecutive_periods`）の書類（届出書 → 有報）→ そのほか（いずれも提出日時の新しい順）
  - 抽出（`edinet/business-results.ts`）: コンテキストは ID が `CurrentYearDuration`・`Prior{N}YearDuration` で始まる duration で、軸なし（連結）か `jppfs_cor:ConsolidatedOrNonConsolidatedAxis` の `NonConsolidatedMember`（単体）だけ。**新規公開の届出書は進行中の期を CurrentYear とするので、表の5期が Prior1〜Prior5**（実データ S100X683 で確認）。中間期・セグメントは読まない（`discardedFacts`）。**期の開始日・終了日はコンテキストの startDate・endDate**（EDINET は末日を書く。決算短信の CurPerEn と同じ）。書類一覧の periodStart は使わない（Sprint 8 の持ち越し S1）。単位は JPY だけ。scale（千円 3・百万円 6）は十進の文字列のまま円に。DEI の連結財務諸表の有無が false の書類で軸の無い事実は単体（連結を作らない会社も NonConsolidatedMember を使う。S100VTA5）
  - **売上高として読む要素**（標準ラベル。最初に値のある要素）: `RevenueIFRSSummaryOfBusinessResults`（売上収益 IFRS）→ `RevenuesUSGAAPSummaryOfBusinessResults`（売上高 US GAAP）→ `RevenueJMISSummaryOfBusinessResults`（売上収益 JMIS）→ `NetSalesSummaryOfBusinessResults`（売上高）→ `OperatingRevenue1SummaryOfBusinessResults`（営業収益）→ `OperatingRevenue2SummaryOfBusinessResults`（営業収入）→ `GrossOperatingRevenueSummaryOfBusinessResults`（営業総収入）→ `OrdinaryIncomeSummaryOfBusinessResults`（経常収益。銀行など）。**読まない要素**: `OrdinaryIncomeLossSummaryOfBusinessResults`（経常利益。英語名が経常収益と紛らわしい）、当期純利益・包括利益・純資産・`NetPremiumsWrittenSummaryOfBusinessResultsINS`（正味収入保険料）など。**営業利益の要素は `OperatingIncomeLossUSGAAPSummaryOfBusinessResults` だけ**（2025年版タクソノミの勘定科目リストで確認。日本基準・IFRS の表には営業利益の行が無いので、EDINET の期の営業利益はふつう NULL＝「記載なし」）。提出者の独自の要素は読まない（`section_not_found`・`revenue_element_unknown`）
  - 結果は `ok`／`no_xbrl`／`section_not_found`（訂正届出書・参照方式の届出書など）／`invalid_values`（円以外・値の食い違い・数でない・期間が不正。一部の期だけを保存しない）。処理済みは取り直さない。**抽出の規則を直して取り直すときは、その書類の `business_results_extractions` の行を消す**（期の行も連鎖して消え、再計算される）
  - **結び付け**（ビュー `edinet_document_codes` の1か所。2つの枝の union all）: 書類の `sec_code`、無ければ `edinet_filers`（提出者の EDINET コード → 証券コード。**書類一覧のすべての行**（半期報告書・臨時報告書・大量保有報告書なども）から、提出日時の新しい行の値で更新する）。社名では結び付けない。新規公開の届出書にも DEI の証券コードがある（実データ）が、一覧の secCode に入るかは未確認
  - **導入時**（R1）: マイグレーションが `prepare_edinet_filers_backfill()` を呼び、既存の書類から `edinet_filers` を作り、`edinet_list_fetched_dates` を全部消す。導入後の最初の約3回の実行は 450 日分の書類一覧の取り直しで、その間は本文（大株主・役員・主要な経営指標等）を処理しない（取り込み状況に「書類一覧を取得しています（残り N 日）」）
  - **期の選び方**（`financial_periods` の1か所）: 候補は決算短信（source `tdnet_summary`、priority 1）と、EDINET の期（`edinet_annual_report` 2・`edinet_registration_statement` 3。抽出が ok・取り下げ・不開示でない・銘柄に結び付く書類）。同じ書類・同じ期の連結と単体は、連結の売上高 → 単体の売上高 → 連結 → 単体の順で1つ。期（終了日）ごとに priority → 提出日時 → 書類IDで1行。**決算短信だけ項目ごとの補い（Sprint 5）、EDINET は選んだ書類の値をそのまま**。追加の列: `source_document_type_code`・`source_submitted_at`・`revenue_element`。未処理の新しい書類は無いものとして扱う（処理した時点で再計算）
  - **再計算のトリガー**（同じトランザクション）: `financial_statements`（既存）、`business_results_periods`・`business_results_extractions`、`edinet_documents` の update（取り下げ・不開示・コード・提出日時・種類）と delete（OLD の sec_code と edinet_filers から。外部キーの連鎖の後に動くことを `business-results.db.test.ts` で確認）、`edinet_filers`（証券コードの無い書類を持つ提出者だけ）、`stocks` の insert（上場前に保存した届出書の期を使う。upsert の既存の行は再計算しない）
  - `financial_metrics_from_periods` は出典を読まない（変えたのは混在の旗を `mixed_consolidation`・`mixed_standard` に分けたことだけ）。補完の情報（`revenue_cagr_supplemented`・`revenue_cagr_period_sources`・`latest_period_source`）は `recalculate_financial_metrics` が関数の結果と `financial_periods` から求める（一時テーブルに対象の銘柄の期を1回だけ読む）
  - 画面: 詳細の5期の表の出典の列（`cell-source`・`data-source`）と書類の列（`cell-document`。書類ID・訂正・提出日・EDINET のリンク）は `components/financials/period-source.tsx`（取り込み状況のカードと共有）。EDINET の期の NULL は「記載なし」（決算短信は「開示なし」）。売上高の名前が「売上高」以外なら小さく示す。CAGR のカードに補った期と混在の注記（「連結・単体が混在」「会計基準が混在」）。直近通期が EDINET で営業利益が無いときの営業利益率の表示は「算出不可（営業利益の記載なし）」（理由のコードは同じ。`describeOperatingMargin` が `latest_period_source` で出し分け）。スクリーニングの売上CAGR の「補完」の印（`components/screening/supplement-mark.tsx`。ホバー・クリック・Enter でポップオーバー。行のクリックに伝えない）。取り込み状況の区画「上場前の期の補完（EDINET）」（`business_results_summary()`）。情報の表示は `info` トークン（`globals.css`）
  - 表示の EDINET の書類の URL・書類種別の名前は `lib/edinet.ts`（取り込みに依存しない）
  - 既知の制限: 書類一覧の期間（450 日）より前の届出書は読まない（最初の有報の表に足りない期がある銘柄は補えないことがある）。売上高と営業収益の両方を記載する会社では EDINET は売上高を読むので、決算短信が営業収益を採る会社では定義がずれうる。提出者の独自の要素だけの売上高は読まない
  - **初回の所要**（キーを設定した後）: 書類一覧の取り直し 約3回、有報（大株主・役員を処理済みのものも主要な経営指標等のために1回ずつ）約 26〜39 回、届出書は1年に数百通（新株予約権・公募増資などの参照方式を含む）。手動の「今すぐ取り込み」を続けて押すと早い
  - 実データの確認（キーなし）: 閲覧サイト（WZEK0040.aspx）の表示から取得したインライン XBRL の抜粋を `edinet/__fixtures__/*-business-results.htm` に置いた（S100X683・S100UOKQ・S100VTA5・S100W7OT・S100DA8H）。訂正届出書・米国基準・9か月の期・失敗の形は `synthetic.ts` で組み立てた
  - テストの接頭辞: E2E（`e2e/business-results.spec.ts`）と `financials/business-results.db.test.ts` は 9V001〜9V010・S9TEST…・E99V…（投入例 `e2e/fixtures/business-results-example.sql`・追加 `business-results-add.sql`・後片付け `business-results-cleanup.sql`）と 9V8xx・S9SEL…・E9SEL…、性能 R0000〜R3999・S9PERF…。取り込みの結合テスト（`ingestion/edinet-business-results.db.test.ts`）は 9V9xx・S9DB…・E9DB…・`list_date` 2003〜2004 年（時計を 2004 年にする）
- **条件④ オーナー企業／社長筆頭株主の自動判定と保有状態の内訳（Sprint 10。F9）**: マイグレーション `20261003000000_surname_readings.sql`（姓の読みの辞書。生成）と `20261003000001_ownership_judgments.sql`
  - **判定は DB の2か所だけ**: 閾値に依存しない分類と内訳は純粋な関数 `ownership_judgment_from_sections(shareholders, officers)`（language sql の1つの問い合わせ。書類・銘柄を読まない。参照するのは辞書 `surname_readings` だけ）、モードと閾値の適用は `screening_evaluate`（`s_owner`・`owner_result`）。画面・API は DB の値を表示するだけで、TypeScript に判定の式は無い。表示の文言（区分名・理由の文・結果のラベル・切り捨て1桁の比率）は `lib/ownership/display.ts` の1か所
  - **保存**: `recalculate_ownership_judgments(codes)`（1つの文で銘柄ごとに純粋な関数を1回呼び、`ownership_judgments`（Sprint 2 の表を拡張）と株主ごとの `ownership_holder_classifications` に保存）。**有報の候補が無い銘柄は行を作らない**（行が無い = `no_annual_report`「有報が未取得」）。評価者・テストは Sprint 8 のテーブル（`edinet_documents`・`annual_report_extractions`・`annual_report_shareholders`・`annual_report_officers`）に入れるだけでよい（判定の表に直接書かない）
  - **再計算のトリガー**（文単位、同じトランザクション）: `annual_report_extractions`・`annual_report_shareholders`・`annual_report_officers` の insert・update・delete（書類の銘柄）、`edinet_documents` の insert・update（取り下げ・不開示・証券コード・期間・提出日時・元の書類・種類）・delete、`stocks` の insert（有報のある銘柄だけ）
  - **使う書類**: Sprint 8 の区画の選び方（`annual_report_sections_for(codes)`。ビュー `annual_report_sections`・`annual_report_candidates` は `…_for(null)` を読むだけになった。銘柄で絞れるので `annual_report_detail(code)` は全銘柄を計算しない（Sprint 8 の m5））。**区画が取り込み待ち（pending）なら、その区画だけ処理済みの直前の有報で判定する（ユーザーの決定）**: 事業年度を問わず、事業年度 → 提出日時の新しい順で、その区画が ok・invalid_values の最初の書類。置き換えた区画は `shareholders_pending_doc_id`・`officers_pending_doc_id`（詳細に「新しい有報は取り込み待ち」の注記）。置き換える書類が無ければ判定不能 `annual_report_pending`
  - **判定不能の理由**（この順）: `no_annual_report` → `annual_report_pending` → `shareholders_not_extracted` → `officers_not_extracted` → `president_not_found`。判定結果は `president_top`（社長が筆頭株主。モードを問わず優先）／`owner_company`（`any` で合計 ≥ 閾値）／`not_matched`／`undeterminable`
  - **正規化**（`ownership_name_key`）: NFKC → 括弧とその中身を除く（閉じていない括弧は末尾まで）→ 空白を除く → 異体字（髙﨑嵜邊邉濵濱齋齊澤櫻廣國德惠榮眞冨嶋嶌槗瀨證）を新字体に → 大文字 → ひらがなをカタカナに。小さな immutable の SQL 関数は、呼び出し側に展開されるよう `set search_path` を付けていない（付けると展開されず、4,000 銘柄の再計算で関数呼び出しの負担が増える。組み込みの関数は pg_catalog が常に先に探される）
  - **社長**（`ownership_title_key` で役職名を正規化。先に「副社長・副頭取・社長補佐・社長室・社長付・社長代行・社長代理・副CEO・CEO補佐」を除く）: ① 代表（代表取締役・代表執行役）＋ 社長・CEO・最高経営責任者・頭取 → ② いなければ代表者全員（`representative`。会長・副社長も候補になる）→ ③ いなければ「取締役社長」等（`title_without_representative`）→ いなければ判定不能。**姓**: 役員の記載の最初の空白の前（連続した空白は1つ。3つ以上の部分がすべて1文字なら決めない）→ 区分1で一致した大株主（個人）の記載。決まらなければ同姓と姓・読みによる資産管理会社の推定をしない（氏名を名称に含む法人は推定する）
  - **区分**: 社長本人 → その他の役員本人 → 同姓の親族（推定）→ 資産管理会社（推定。社長の氏名 → 姓（1文字の姓は法人格の語を除いた先頭だけ）→ 読み（3文字以上）→ ローマ字（4文字以上、単語として）の順）→ オーナー系以外。法人かどうかは `ownership_is_corporate`（「常任代理人」で始まる括弧は見ない）、金融機関・信託口・持株会・公的機関は `ownership_is_excluded_corporate` で区分4から除く。語の一覧は関数の中の1か所（増やすのはよい）
  - **姓の読みの辞書**: `scripts/data/surname-readings.txt`（約1,400 姓）から `pnpm tsx scripts/generate-surname-readings.ts` でマイグレーションを生成する（適用済みの生成物は書き換えず、追加の読みは新しいマイグレーションで足す。Sprint 11 で 岩崎イワザキ・宮崎ミヤサキ・中沢ナカサワ・小島オジマ／コシマ・塩谷シオタニ／エンヤを `20261004000000` に追加。手で編集しない。ローマ字は `lib/ownership/romaji.ts` がヘボン式の3つの形（そのまま・長音を省く・長音を H）で作る）。出典は名字のランキングを参考にした一般的な読みで、1件ずつの照合はしていない
  - **比率**: 合計は numeric のまま。一覧（要約・ポップオーバーの筆頭株主を含む）と詳細の区分別の合計は百分率の小数点以下1桁に**切り捨て**（`formatTruncPct`、DB の `trunc(x, 1)`）、詳細の株主ごとの比率は記載どおり（`formatRatioPct`）。閾値の比較は保存値で `合計 >= 閾値`
  - **URL**: `owner`（閾値 %、0〜100、既定 20）・`ownermode`（`any`／`president`）・`undeterminable=include`（④の判定不能を含める。「算出不可を含める」（①〜③）とは独立）・`off` に `owner`・`sort=owner`（判定不能は常に最後）。正規形の順は `cagr, margin, years, owner, ownermode, off, unavailable, undeterminable, market, sector, sort, order, page`（`owner`・`ownermode` は常に書く）。**既定で条件④がオンなので、①〜③だけを確かめるテストは `off=owner` を付ける**（有報の無い投入例の銘柄は判定不能で除かれる）
  - 画面: 一覧の列「条件④（自動判定）」（`components/screening/owner-cells.tsx`）と「保有状態（オーナー系合計）」（積み上げバー `components/ownership/ownership-bar.tsx`。色は `globals.css` の `owner-*`）。ポップオーバーは共通の `HoverPopover`（`components/screening/hover-popover.tsx`。Sprint 9 の「補完」の印も使う。title 属性を付けない）。市場区分と業種は1列に2段（並べ替えのボタンは2つ）。スクリーニングの画面だけ幅を `max-w-7xl` にする（`data-layout="wide"` を `has-[]` で見る。ヘッダーとフッターも合わせる）。詳細は「条件④ 判定根拠」と「保有状態の内訳」（`components/stocks/ownership-sections.tsx`）と、条件の判定の④の行。取り込み状況の有報の区画に「条件④を判定できた銘柄」（`annual_reports_summary()` の `ownership`）
  - 性能（4,000 銘柄・有報 6,000 通・大株主と役員 各10名）: 全件の再計算 約4.3秒、1銘柄 約7ms、1通の取り下げ 約7ms、`annual_report_detail` 約3ms、`screen_stocks` 約25ms（`ownership.db.test.ts` が 10秒・50ms・50ms・10ms・100ms 以内を検査）
  - 既知の制限: 辞書に無い姓はカタカナ・ローマ字の名称と照合しない。3文字以上の読みでも外来語に含まれて誤って一致する（新井「アライ」と「○○アライアンス」）。姓の一致による推定なので、旧姓・婚姻による姓の違い、社長以外の役員の姓、登記上の資本関係は見ない。代表者による補いでは会長・副社長も社長候補になる。新しい有報が取り込み待ちの間は直前の有報で判定する（事業年度が1つ古いことがある）
  - テストの接頭辞: E2E（`e2e/ownership.spec.ts`）と `ownership/ownership.db.test.ts` は 9U001〜9U014・SXTEST…・E99U…（投入例 `e2e/fixtures/ownership-example.sql`、後片付け `ownership-cleanup.sql`）と 9U8xx・SXSEL…・E9SX…、性能 Q0000〜Q3999・SXPERF…。ダッシュボードの E2E は SDASH…
- **条件④の手動補正（Sprint 11。F10）**: マイグレーション `20261004000000_ownership_overrides.sql`。部品は `components/stocks/owner-override.tsx`（詳細の「条件④ 判定根拠」の先頭）、API は `app/api/stocks/[code]/ownership-override/`（`GET`・`PUT`・`DELETE`、`acknowledge/` の `POST`）
  - **補正は利用者のデータ**（市場データではない）: `public.ownership_overrides`（主キー (user_id, code)。`auth.users`・`stocks` への参照は削除で連鎖）。**「書き込みは service_role のみ」の例外**で、authenticated に select・insert・update・delete だけを grant し、RLS（`user_id = (select auth.uid()) and (select current_user_is_allowed())`。4つの操作すべて）で本人の行に限る。書き込みは利用者自身のセッション（サービスロールを使わない。user_id をアプリのコードに決めさせない）。`e2e/db-privileges.spec.ts` は authenticated が書き込めるテーブルをこれだけに固定している
  - **直接の書き込み（PostgREST）への防御**: BEFORE INSERT OR UPDATE の行トリガー `ownership_overrides_before_write` が、補正時の自動判定の記録（`auto_status`・`auto_undeterminable_reason`・`auto_president_is_top_holder`・`auto_owner_total_pct`・`auto_shareholders_doc_id`・`auto_officers_doc_id`）を常に `ownership_judgments` から求め直す（行が無ければ「有報が未取得」）。insert の `user_id` は NULL のときだけ `auth.uid()`（違う値は残して RLS の with check で拒否させる）。`created_at` は insert で now()・update で元の値、`updated_at` は now()。`user_id`・`code` の変更は 42501 で拒否。メモの前後の空白を除く。check 制約: `verdict`（3択）、メモ（1〜1,000 コードポイント、前後に空白なし）。トリガー関数の EXECUTE の権限は発火に関係しない（authenticated からは revoke 済み。security invoker なので、許可ユーザーだけが判定を読める）
  - **メモの規則**（画面・API・DB で同じ）: 空白は JavaScript の `\s` と同じ集合（DB は `[\t\n\v\f\r    -     　﻿]` と明示。`[[:space:]]`・`btrim` の既定は使わない）。保存は前後の空白を除いた値、1〜1,000 コードポイント（`lib/ownership/memo.ts`。`String.length` は使わない）。**textarea に `maxLength` を付けない**（UTF-16 の単位で数えるため「𠮷」1,000 個が入らない）
  - **補正の選択肢は3択（ユーザーの決定）**: `president_top`／`owner_company`／`not_matched`（自動判定の結果と同じ名前）。**モードは当て、閾値は当てない**（「社長が筆頭株主のみ」では `owner_company` の補正は満たさない）
  - **判定の式は DB の1か所**: `owner_result_of(status, is_top, total, mode, threshold)`（自動判定の結果。現在の判定と、補正時の記録の両方に使う）と `owner_status_of(result, mode)`（条件④の状態）。`screening_evaluate` が呼び出したユーザーの補正（`auth.uid()`）を結合し、`owner_auto_result`（自動判定）・`owner_override`（補正）・`owner_result`（補正後）・`s_owner`（補正後の状態）を返す。補正があれば判定不能（`unavailable`）にならない。並べ替え `sort=owner` は自動判定の合計のまま（補正で結果に入った判定不能の銘柄は最後）。ダッシュボード・取り込み状況の「条件④を判定できた銘柄数」は自動判定のまま（利用者ごとに変えない）。小さな SQL 関数は展開されるよう `set search_path` を付けていない
  - **「補正後に自動判定が更新されました」**（`auto_changed`）: `owner_override_summary(code, params)` が、記録と現在の自動判定の項目を `is distinct from` で比べる（`judged_at`・取り込み待ちの書類は比べない。書類IDだけが変わっても知らせる。元に戻れば消える）。「確認済みにする」（`owner_override_acknowledge`）と編集の保存は、記録を現在の自動判定で置き換える
  - **応答**: `ownership` に `result`（補正後）・`auto_result`（自動判定）・`override`（`verdict`・`memo`・`created_at`・`updated_at`・`auto_changed`・`auto_at_override`（補正・編集・確認済みの時点の自動判定の記録。`result` は現在のモード・閾値で求める）・`auto_current`（現在の自動判定の同じ項目。知らせの表の「現在」）。一覧（`GET /api/screening`）・詳細（`GET /api/stocks/[code]`）・書き込み（`PUT`・`POST …/acknowledge`）のどれも同じ形。無ければ null）。DB の値の形が違えば黙って null にせず 500 `internal_error`（`apiOverrideResponse`・`OverrideShapeError`。Sprint 11 評価の m6）。詳細の `evaluation` に `ownerAutoResult`・`ownerOverride`。API は日時を日本時間の ISO にする（`lib/ownership/override.ts`）。書き込みの API は同一オリジンだけ（403 `cross_origin`）、検証の失敗は 400 `invalid_body`／`invalid_override`（`fields`）、無い銘柄は 404、補正が無い確認済みは 404 `override_not_found`
  - **画面**: 補正後の判定には「手動補正」のラベル（`ManualOverrideLabel`。紫の `manual` トークンと鉛筆のアイコン。「自動判定」の `info` と区別）と元の自動判定を並べる。詳細の `ownership-evidence` の `data-result` は自動判定、`data-effective-result` は補正後。一覧の `cell-owner-judgment` の `data-result` は補正後、`data-auto-result` は自動判定、`data-override`。保存・取り消し・確認済みの後は `router.refresh()`（状態の切り替えと同じ遷移で。クライアントのルーターのキャッシュも捨てるので、戻る・パンくず・ヘッダーのどれでもスクリーニングに補正後の結果が出る）。取り消しは確認のダイアログ（`components/ui/alert-dialog.tsx`）
  - 区分名の表記は一覧と詳細で `CategoryName` の1つ（文字列は「資産管理会社（推定）」、「（推定）」の部分を推定のラベル `estimated-label` として描く。Sprint 10 評価の m6）
  - **上場廃止で `stocks` の行を消さないこと**（消すと利用者の補正とメモが連鎖して黙って消える。Sprint 12 は `stocks.delisted_on` で表し、行を残す）
  - 保存・取り消しの後は、元のボタンが消えるので、次の状態の操作（「編集」・「判定を手動で補正する」）にフォーカスを移す（Sprint 11 評価の m3）。条件④がオフのとき、④ の印の title に「（手動補正）」を付けない（m2）
  - 既知の制限: 補正の履歴は持たない（最新の1つだけ）。複数のタブで同時に編集すると後の書き込みが勝つ。大株主ごとの区分は補正できない（銘柄の判定単位）
  - テストの接頭辞: E2E（`e2e/ownership-override.spec.ts`）は Sprint 10 の投入例（9U001〜9U014・SXTEST…）と、`e2e/fixtures/ownership-override-add.sql`（SYTEST16。9U006 の訂正有報）・`ownership-override-add-9u004.sql`（SYTEST04）、後片付け `ownership-override-cleanup.sql`（SYTEST…）。`ownership/ownership-override.db.test.ts` は同じ投入例と 9T…・SYDB…、性能 T0000〜T3999・SYPERF…。補正の行は銘柄の削除で連鎖して消える
- **日次取り込みの信頼性（Sprint 12。F11）**: マイグレーション `20261005000000_ingestion_reliability.sql`
  - **実行の行の列**: `stopped_reason`（`time_budget`／`stale`／`rate_limited`／`unauthorized`／`redirect`／`consecutive_failures`／`save_failed`／`delisting_held`／NULL）・`remaining_count`／`remaining_unit`（`stocks`・`disclosure_dates`・`list_dates`・`documents`）・`failed_count`（全数）・`last_progress_at`（最後に保存した時刻。保存の関数が書く表（`stock_listing_dates`・`financial_fetched_dates`・`edinet_list_fetched_dates`・`annual_report_extractions`・`business_results_extractions`）の文単位のトリガー `private.touch_ingestion_progress` と、`complete_stock_master_run` が更新する）。取り込みは `finishRun(…, outcome)`（`lib/ingestion/finish.ts`）で `finish_ingestion_run(…, p_outcome)` に渡す
  - **「一部完了」と「一部失敗」**: status は4値のまま。`partial` で `failed_count = 0` かつ `stopped_reason` が `time_budget`・`stale` なら一部完了、ほかの `partial`（新しい列の無い過去の行を含む）は一部失敗。規則と表示は `lib/ingestion/runs.ts` の `partialKindOf`・`runStatusLabel`・`formatRemaining` の1か所。バッジは `RunStatusBadge` の `partialKind`（`data-partial-kind`）
  - **応答の無い実行の後片付け**（`start_ingestion_run`）: 処理済みがあれば `partial`（`stale`。「応答が無くなったため中断されたものとみなしました（15 分以上）。保存済みの分は残っています。…」）、0件なら今までどおり `failed` と旧い文言
  - **失敗した対象**: `public.ingestion_run_failures`（実行ごと・対象ごとに1行、1実行 1,000 行まで。`item_type` は `stock`・`disclosure_date`・`list_date`・`document`、`code` は参照にしない）。保存は理由のコード（`reason`・`http_status`・`network_error`）だけで、文言は `lib/ingestion/failures.ts` の `failureMessage`（応答の本文・URL・キーは保存しない）。株価にも「5銘柄続けて失敗したら打ち切る」を加えた。財務は開示日ごとの取得なので失敗の単位は開示日（その日に開示したすべての銘柄が未取得）
  - **呼び出しの制限**（`lib/ingestion/pacer.ts` の `createPacer`。すべての外部 API の要求が通る）: 間隔（株価・銘柄マスタ 600ms、財務 1,100ms、EDINET 1,000ms。前の要求の開始から）と期限（210 秒）に加え、制限の応答（J-Quants の 429、EDINET の 429・503）は Retry-After（秒の整数。1〜120 秒に収める。無い・HTTP 日付なら 15→30→60 秒）待って同じ要求を再試行する。1つの要求につき3回まで、回復後はその実行の残りで間隔を2倍、待ちの終わりが期限を超えるなら待たずに打ち切る（`rate_limited`）。記録は `details.rateLimit`（`hits`・`retries`・`waitedMs`・`exhausted`）と `details.apiCalls`
  - **実行の詳細**: 画面 `/imports/runs/[id]`（`src/app/(app)/imports/runs/[id]/`、部品 `components/imports/run-detail-view.tsx`。ナビゲーションの項目ではないが `/imports` の配下なので「取り込み状況」に `aria-current`）と `GET /api/ingestion/runs/[id]`（`lib/ingestion/run-detail.ts`。id は正の整数だけ（`parseRunId`）、形が違えば 400 `invalid_id`・無ければ 404。画面は not-found.tsx の 404）。実行履歴の表は列を足さず、「開始」の日時を詳細へのリンク（`run-detail-link`）にした（`dashboard.spec.ts` が見出しの7列を完全一致で比べている）
  - **データの鮮度の警告**: DB 関数 `data_freshness(p_now default now())`（authenticated・security invoker）の1か所。target ごとに `succeeded`・`partial` の最新の終了（stale の行は `last_progress_at`。後片付けの時刻は使わない）を求め、48 時間以上なら古い。**保存が1件でもあれば更新とみなす**（毎日 `rate_limited` で少しだけ保存して止まる状態は警告にならない。実行履歴の「一部失敗」で見える）。一度も更新していない target は警告しない。警告（`components/shell/stale-data-warning.tsx`）は `ProtectedShell` に置き、ログイン後の全画面（global-not-found を含む）のヘッダーの直下に出す。1行目は古い target のうち最も古い日時、内訳は 640px 未満で `<details>` に折りたたむ（`stale-data-targets.tsx`）。取得に失敗したら警告を出さずに描画を続ける（ログだけ）。ダッシュボードは「データの鮮度を確認できませんでした」、`GET /api/dashboard` は `freshness: null`。**レイアウトに置くので、クライアント遷移では更新されない**（リロード・`router.refresh()` で更新）。リンク名は既存の E2E のロケーターと衝突しないよう「実行履歴を確認」（「取り込み状況」「スクリーニング」を含めない）、`role="status"` は使わない
  - **取り込み中の検索**（AC11.5。ユーザーの決定: 銘柄単位の整合＋注記）: 保存の単位（銘柄マスタ全体・初出日 50 銘柄・開示日 1 日・書類一覧 1 日・書類 1 通）ごとに、保存と指標・判定の再計算が1トランザクション（`ingestion-consistency.db.test.ts` で5つの関数を確かめる）。実行中はスクリーニングと詳細に `ingestion-running-note`、target ごとの最新の終了済みの実行（`failed` で残りの無い実行は飛ばす）に残りがあればスクリーニングに `ingestion-remaining-note`（「未取得の残りがあります（財務 20 日分・…）」。`data_freshness` の `remainingCount`・`remainingUnit`）。部品は `components/imports/ingestion-notes.tsx`
  - **上場廃止**: `stocks.delisted_on`（上場廃止を確認した銘柄マスタの日付。NULL = 上場中）。銘柄マスタの成功で、取り込み対象の行（Sprint 3 の規則）に無かった上場中の銘柄に付け、再び現れたら NULL に戻す（`complete_stock_master_run`）。**行は消さない**。1回で新たに消えた銘柄が 100 を超えたら反映を保留（`partial`・`delisting_held`）。判定は `screening_evaluate` の `is_delisted` の1か所で、スクリーニングは条件・含める設定に関係なく除き、件数（`stockCount` は上場中の数。`delistedCount`）・絞り込みの選択肢の数にも数えない。詳細は 404 にせず「上場廃止」のラベル（`delisted-badge`）と「含まれない（上場廃止）」（`evaluation.delisted`）。株価の初出日（`listing_dates_pending`）と EDINET の本文（`edinet_ingestion_state`）の対象から除く。ダッシュボードに「うち上場廃止 N 銘柄」。手動補正は残り、再上場で使われる
  - **大量の上場廃止の保留を解く手順**（本当に 101 銘柄以上が消えた場合。毎日 `delisting_held` が続く）: 1) 最新の銘柄マスタの日付 D を確かめる（`select max(listed_info_date) from stocks`）2) 消えた銘柄を確かめる（`select code, company_name, listed_info_date from stocks where delisted_on is null and listed_info_date < D`）3) J-Quants・JPX の上場廃止の情報と見比べてから、`update stocks set delisted_on = D where delisted_on is null and listed_info_date < D` を service_role（SQL エディタ）で実行する。次の銘柄マスタの取り込みからは通常どおり
  - 応答の無い一部完了（`partial`・`stale`）で残りを数えていない実行は、実行履歴に「残り 不明」、詳細に「不明」（`isRemainingUnknown`。Sprint 13 で追加）
  - 既知の制限: 関数が強制終了された実行（stale）は、失敗の行が一部欠けることがある（行は実行の終了時に保存する）。上場廃止の日付は「銘柄マスタで確認した日」で、J-Quants の上場廃止日ではない。取り込み状況の「取り込み待ちの書類」の数には、上場廃止の銘柄の書類も含む（本文の取得はしない）
  - テストの接頭辞: E2E（`e2e/ingestion-reliability.spec.ts`）は 9N001〜9N004・書類 S12NTST1（投入例 `e2e/fixtures/ingestion-reliability-example.sql`、実行は `details ->> 'fixture' = 'sprint-12'`、後片付け `ingestion-reliability-cleanup.sql`）。`ingestion/ingestion-consistency.db.test.ts` は 9N8xx・S12NDB…・性能 N0000〜N3999・実行の `fixture` = `sprint-12-db`、取り込みの結合テスト `ingestion/ingestion-reliability.db.test.ts` は 9N9xx
- **条件プリセット（Sprint 13。F12）**: マイグレーション `20261006000000_screening_presets.sql`。部品は `components/screening/preset-bar.tsx`（結果の列の先頭。狭い画面では条件の要約の直下。Sprint 14 でダイアログごとに `preset-save-dialog.tsx`・`preset-manage-dialog.tsx`・`preset-row-item.tsx`・`preset-overwrite-body.tsx`・`preset-shared.tsx` に分けた）、ロジックは `lib/screening/presets.ts`（client・server 共通）・`preset-queries.ts`（読み出し。server-only）・`preset-api.ts`（API の検証・エラーの分類）、API は `app/api/screening/presets/`（`GET`・`POST`、`[id]/` の `PATCH`・`DELETE`）
  - **中身は正規形のクエリ文字列**（`serializeScreeningParams` の出力から page を除いたもの。例 `cagr=20&margin=10&years=5&owner=20&ownermode=any&market=0113&sort=cagr&order=desc`）。状態の正本は URL で、プリセットは「名前の付いた URL」。条件の文法・範囲の検証は `parseScreeningParams` の1か所だけ（API は厳しく、読み出しは URL と同じ寛容な解釈）。DB の check は**形だけ**（パラメータの並び・名前を固定した全体一致の正規表現。値は文字クラスと長さだけ。`%` `#` `/` `\` `?` 空白 改行 非 ASCII・重複・page は入らない）で、範囲外の値（`cagr=99999`）は通す。保存したクエリはそのまま `/screening?` の後ろに付けて URL にするので、この文字クラスがリダイレクトの安全性の根拠。無効な項目のある・正規形でないプリセット（直接の書き込みでだけ入りうる）は、印と注記を出し、適用すると既定値・正規形にし、「適用中」とはみなさない（`parsePresetQuery` の status）
  - **利用者のデータ**（`ownership_overrides` と同じ方針。「書き込みは service_role のみ」の2つ目の例外）: authenticated に select・insert・update・delete だけ、RLS は4つの操作すべてで本人かつ許可ユーザー。BEFORE INSERT OR UPDATE のトリガー `screening_presets_before_write`: insert の user_id は NULL のときだけ `auth.uid()`、`id`・`user_id` の変更は 42501、`created_at` は固定、`updated_at` は名前かクエリが変わったときだけ新しくする（既定の切り替えでは変えない）、名前の前後の空白を除く、1人 50 件まで（SQLSTATE `QP050`）。名前は 1〜40 コードポイント・途中の制御文字と U+2028・U+2029 は不可・同じユーザーの中で一意（`screening_presets_user_name_key`）。既定は各ユーザー0か1個（部分一意索引 `screening_presets_one_default_idx`）。空白の文字集合は `lib/text/whitespace.ts`（メモと共有）
  - **書き込みの関数**（security invoker）: `create_screening_preset(name, query, default)`・`update_screening_preset(id, name, query, default)`（NULL は変えない）・`set_default_screening_preset(id, default)`。どれも**ユーザーごとの advisory lock**（`hashtextextended('screening_presets:' || user_id, 0)`。insert の上限の確認と同じキー）→ 対象の行を `for update` → 無ければ（存在しない id・他人の行）何も変えずに NULL（API は 404）→ ほかの行の既定を外してからこの行を既定に、の順で1トランザクション。同時の切り替えは直列になり、後にコミットした方が既定。API は 23505 を制約の名前で区別する（名前 → 409 `duplicate_name`、既定の索引 → 500）
  - **既定のプリセット**: URL に条件のパラメータ（`SCREENING_PARAM_KEYS`。page を含む。`hasScreeningParams`）が1つも無いとき、`/screening` のページが `redirect()` で `/screening?<保存したクエリ>` へ送る（`requireAllowedUser()` の後。check で必ず `cagr=` から始まるのでリダイレクトは1回）。ヘッダーの「スクリーニング」も `/screening` なので、表示中に押すと既定のプリセットの条件に戻る（AC12.4 の解釈）。詳細を開いている間のヘッダーの「スクリーニング」は今までどおり直前の条件（AC7.6）。条件のパラメータの無い銘柄詳細は既定のプリセットで判定する（`detailConditionsWithPreset`。「既定のプリセット『X』の条件で判定しています」）。**API（`GET /api/screening`・`GET /api/stocks/[code]`）は既定のプリセットを当てない**（呼び出し側が条件を明示する。画面の詳細と API の判定は違いうる）。プリセットを読めなければリダイレクトせず標準の条件で描画し注記、一覧は「プリセットを読み込めませんでした」（0件として扱わない）、保存は無効、`GET /api/screening/presets` は 500
  - **画面の操作**: 適用は `ScreeningView` の `replaceAll`（`router.replace`。履歴を増やさない。待っている書き換えは取り消し、条件パネルを作り直す）。「適用中」は URL との完全一致だけで決める（作成の古いプリセット → 標準の条件 → 保存されていない条件）。保存・上書きするのは表示中の結果の条件（待っている書き換えは `flushQueued` で発行し、更新まで確定のボタンを無効）。入力欄のエラーは `ThresholdField` の `onInvalidChange` で上げ、保存のダイアログに「入力中の無効な値は保存されません」。書き込みの後は `router.refresh()`、ただし条件のパラメータの無い URL のままだと既定を作ったときにリダイレクトされるので、そのときは表示中の条件の URL に `router.replace` する。「既定の条件に戻す」は既定のプリセットがあればその条件。ダイアログは状態で開くので、閉じたら開いたボタンにフォーカスを戻す（削除した行なら管理のダイアログ）。E2E では、Radix のメニュー・ダイアログの開閉のアニメーションの途中の Esc・クリックが別の層に届くので、閉じ終わるのを待ってから次の操作をする（`e2e/screening-presets.spec.ts` の `escape()`・`openMenu()`）
  - **入力欄の確定とプリセットの適用**（Sprint 13 評価の m1）: セレクター（メニュー）を開くと入力欄から離れるので、その時点で入力は確定し（Sprint 6 の「入力欄から離れたら確定」）、URL に一時的に書かれることがある。最終の URL と結果は選んだプリセットで、履歴は増えない
  - 確認のダイアログ（削除・上書き）が閉じるアニメーションの間の Esc は、閉じかけの確認のダイアログに届くので、管理のダイアログを閉じる操作として扱う（`closingEscape`。Sprint 13 評価の m2）
  - 既定の条件の解決は Sprint 14 から `lib/screening/default-conditions.ts` の1か所（下の「ウォッチリストと新規該当」）
  - テスト: E2E は `e2e/screening-presets.spec.ts`（市場データは `ownership-example.sql`、プリセットは `screening-presets-example.sql`（owner の3件）、後片付け `screening-presets-cleanup.sql`。C4-11 で authenticated の select を一時的に revoke し、`afterAll` でも `grant select` だけを戻す）。`lib/screening/presets.db.test.ts` は名前の接頭辞「〔DB13〕」と `@quantis-db13.local` のユーザー（性能・連鎖）。同時実行のテストは2つの接続で advisory lock の待ちを `pg_stat_activity` で確かめる
- **ウォッチリストと新規該当（Sprint 14。F13）**: マイグレーション `20261007000000_watchlist_and_changes.sql`。スプリント計画の最後のスプリント
  - **ウォッチリスト**: `public.watchlist_items`（主キー (user_id, code)。`auth.users`・`stocks` への参照は削除で連鎖。上場廃止では消えない）。**利用者のデータ（「書き込みは service_role のみ」の3つ目の例外）**: authenticated に select・insert・update・delete だけ、RLS は4つの操作すべてで本人かつ許可ユーザー。BEFORE INSERT OR UPDATE のトリガー `watchlist_items_before_write`（VOLATILE のまま。ロックの後に確定した行を見るため）: insert の user_id は NULL のときだけ `auth.uid()`、`user_id`・`code` の変更は 42501、`created_at` は固定、`updated_at` はメモが変わったときだけ、メモは前後の空白を除き空なら NULL。1人 500 件（SQLSTATE `QW500`）で、**ユーザーごとの advisory lock（`'watchlist_items:' || user_id`）の後、同じ (user_id, code) の行が既にあれば上限の確認を飛ばす**（PostgreSQL は BEFORE トリガーを ON CONFLICT の重複の確認より先に実行するので、飛ばさないと冪等な追加が上限のエラーになる。契約の R1）。メモは 0〜1,000 コードポイント（前後の空白を除いた後で数える。`lib/watchlist/memo.ts`）
  - 画面: `/watchlist`（ナビゲーションの「ウォッチリスト」。部品は `components/watchlist/`。表は `watchlist-view.tsx`、星は `watchlist-toggle.tsx`（スクリーニングの行のコードの列・銘柄詳細の見出し。API が成功してから切り替え、メモのある銘柄は確認のダイアログ、押しても行の遷移をしない））。表示の値は `screen_stocks` の行と同じ形（DB 関数 `screening_rows(params, codes)`。スクリーニングのページの行も同じ関数で作る）、判定は既定の条件。表の枠は `relative`（sr-only のラベルなど絶対配置の子が枠の外に出て、ページが横スクロールしないように）
  - API: `GET /api/watchlist`（条件のパラメータが無ければ標準の条件。既定のプリセットは当てない）、`PUT`（冪等。新規 201・登録済み 200）・`PATCH`（`{"memo"}`）・`DELETE /api/watchlist/[code]`。コードは `normalizeStockCode`、400 `invalid_code`・404 `stock_not_found`（PUT）・404 `not_found`・409 `watchlist_limit`・400 `invalid_memo`／`invalid_body`。`GET /api/screening` の行に `watchlisted`・`isNew`、全体に `comparison`・`newCount`。`GET /api/stocks/[code]` に `watchlist`
  - **比較の基準の記録**: `screening_snapshots`・`screening_snapshot_stocks`（市場データと同じ RLS。authenticated は select だけ）。**判定の結果ではなく入力**（指標・初出日・④の自動判定の入力・市場・業種・上場廃止・社名と、記録の時点の基準日）を残すので、条件（閾値・プリセット）を変えても比べ直せる。**記録するのは定期実行の日次の取り込み（`/api/cron/daily`）の銘柄マスタの開始だけ**（ユーザーの決定。`start_ingestion_run('stock_master','cron')` が実行の行を作れたとき、同じトランザクションで `capture_screening_snapshot(run_id)` を呼ぶ。記録の日時は `now()` で実行の開始と同じ）。手動の実行・財務・EDINET・409 では記録しない（ローカルでは cron が動かないので、`/api/cron/daily` を CRON_SECRET 付きで呼ぶか、psql で `select public.capture_screening_snapshot()`）。最新 7 回だけを残し、比較には最新の1回を使う
  - **記録の失敗で取り込みを止めない**（R2）: 記録の呼び出しは `start_ingestion_run` の中のセーブポイント（`begin … exception when others`）で包み、失敗しても実行は始め、`details.snapshot = "failed"`（成功は `"captured"` と `snapshotId`）を内側のブロックの外で書く。`query_canceled` は捕まえない（開始ごと失敗する。受け入れる）。実行の詳細に `run-snapshot-failed`。**終了の関数（`finish_ingestion_run`・`complete_stock_master_run`）は details を置き換えるとき `snapshot`・`snapshotId` だけを残す**（`ingestion_run_kept_details`。開始時の値が勝つ。R5）
  - **判定の1か所**（R3）: `screening_evaluate(params, codes, snapshot_id)` が条件ごとの状態に加えて `included`・`exclusion`（`delisted` → `filters` → `unmet` → `unavailable`（①〜③）→ `undeterminable`（④）の優先順位で1つ）・`blocking`（該当を妨げている条件。①〜④の順、`[{condition, status}]`）を返す。`screen_stocks`（結果と除外の件数）・`stock_detail`・`screening_changes`・ウォッチリストはこの分類を使い、式を持たない。記録の判定も同じ関数で、**入力の行の取り出し元（`screening_evaluate_input(codes, snapshot_id)`。分岐した方の表だけを読む）と基準日だけを切り替える**。銘柄詳細の `evaluation-inclusion` の `data-kind` は互換のため判定不能も `unavailable`（`detailInclusionKind`。m7）。TypeScript は文言の対応だけ（`describeInclusion`・`watchlistInclusionText`）
  - **新たに該当・外れた**: `screening_changes(params)`（authenticated・security invoker）が、最新の記録と今のデータを同じ条件・**今の手動補正（ユーザーの決定。補正による変化は出さない）**で判定して比べる。`status` は `ok`／`no_snapshot`／`empty_snapshot`（記録の上場中が0件なら比較しない）。理由は `new_stock`・`relisted`・`delisted`・`missing`（どれもそれだけ）、`filters`、条件の変化 `{kind: condition, condition, from, to}`（片方の blocking にだけある条件）。表示の文言は `lib/screening/changes.ts`
  - 画面: ダッシュボードの区画「前回の取り込みからの変化」（`components/dashboard/screening-changes.tsx`。既定の条件で比較。各一覧は 50 行まで、今回の取り込みの実行中・未完了の注記、ウォッチリストの星の印は比較の関数の外で別に読む）、スクリーニングの NEW（表示中の条件で判定。`new-badge`・`new-count-note`）、ウォッチリストの NEW・外れた。どれも読めないときは 0 件として扱わずに注記（スクリーニングは星・NEW を結果の検索と別の問い合わせにして、片方の失敗で表を失わない）
  - **既定の条件の解決は1か所**: `lib/screening/default-conditions.ts`（`defaultConditionsFrom`・`defaultConditionsFromList`）。ダッシュボード・ウォッチリスト・`/screening` のリダイレクトが使う（銘柄詳細は `detailConditionsWithPreset` がプリセットの解釈を受け取る）。既定のプリセットに無効な項目・正規形でない形があれば `default-preset-invalid-note`（Sprint 13 評価の m3）。**API は既定のプリセットを当てない**
  - 性能（4,000 銘柄、authenticated）: 記録 約 20ms、比較 約 35ms、スクリーニングの表示の合計 約 65ms、ウォッチリスト 500 件 約 80ms（`screening-changes.db.test.ts`）。ウォッチリストのため、`owner_override_summary` を language sql から plpgsql にした（`ownership_summary` の中から行ごとに呼ぶと sql の本文は毎回計画し直される。本文と結果は同じ）
  - テスト: E2E は `e2e/watchlist.spec.ts`・`e2e/screening-changes.spec.ts`（市場データは `ownership-example.sql`、記録の後の変化 `screening-changes-after.sql`、ウォッチリスト `watchlist-example.sql`（owner の4件）、後片付け `watchlist-cleanup.sql`・`screening-snapshots-cleanup.sql`。上限のテストは銘柄 W0000〜W0499、50 行のテストは C0001〜C0051。記録を失敗させる制約 `e2e_fail_snapshot` と revoke は `finally`・後片付けで必ず戻す）。`lib/watchlist/watchlist.db.test.ts`（9S5xx・9S8xx）、`lib/screening/screening-changes.db.test.ts`（9S9xx・性能 S0000〜S3999、記録0件が前提）
- **同一オリジンの確認**（手動取り込み・ログアウト）: `lib/http/same-origin.ts`。`Origin` のホストを `X-Forwarded-Host`（最初の値）または `Host` と比べる（Server Actions と同じ）。`request.nextUrl.origin` はサーバーの既定のホスト名になるので使わない。拒否は 403 `{"error":"cross_origin"}`。`GET /auth/signout` のリダイレクトは相対パス（別のホスト名で開いた利用者を localhost に移さない）。dev を 127.0.0.1 で開けるよう `next.config.ts` に `allowedDevOrigins` を設定している。
- **Vercel での設定**: `vercel.json` の Cron は `/api/cron/daily` を `0 11 * * *`（UTC。毎日 20:00 JST）に、`/api/cron/financials` を `0 13 * * *`（22:00 JST）に、`/api/cron/edinet` を `0 15 * * *`（0:00 JST）に呼ぶ。Cron は本番のデプロイでだけ動き、Hobby プランでは最大 59 分ずれる。画面の表示（`lib/ingestion/schedule.ts`）と `vercel.json` の一致は `schedule.test.ts` が確かめる。Vercel のプロジェクトの環境変数に、Supabase の3つ（本番の値）と `JQUANTS_API_KEY`、`EDINET_API_KEY`、`CRON_SECRET`（`openssl rand -hex 32` などで生成した16文字以上）を設定する。
- **許可の取り消しの理由（N1）**: クライアント遷移中にガードが `/auth/signout?reason=revoked` へ送ると、ルーターがこの URL を同時に2回要求することがある。後の要求は「未ログイン」になるため、セッションの Cookie を持っていて `reason=revoked` のときは、未ログインでも `/login?reason=revoked` へ送る（理由は表示にしか使わない）。
- ローカルの環境変数: `NEXT_PUBLIC_SUPABASE_URL`、`NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY`、`SUPABASE_SECRET_KEY`、`JQUANTS_API_KEY`、`EDINET_API_KEY`、`CRON_SECRET`（`.env.example`。`pnpm env:local` は Supabase の3つだけを書き換え、ほかの行は残す）

## 開発ハーネス（planner → generator → evaluator）

起動: `/harness <1〜4行のプロンプト>`（`.claude/skills/harness/SKILL.md`）。メインセッションはオーケストレーターに徹し、自分では実装もテストもしない。

- `planner`（`.claude/agents/planner.md`）: プロンプトを `docs/harness/spec.md` に展開する。「何を作るか」だけを書き、実装の詳細（DB スキーマ、ライブラリ、API 設計）は書かない。受け入れ基準は、ブラウザ操作で合否を判定できる形にする。
- `generator`（`.claude/agents/generator.md`）: 1スプリントで1機能を実装する。モードは契約・実装・修正の3つ。スタブやダミーデータで動いているように見せることは禁止。git リポジトリであれば、スプリントごとに `sprint-NN: <機能名>` でコミットする。
- `evaluator`（`.claude/agents/evaluator.md`）: Playwright MCP（`.mcp.json`）で実際に操作し、画面・API・データベースの3段階で検証する。ソースコードは編集しない（Edit 禁止）。

サブエージェント同士は直接会話できない。受け渡しはすべて次のファイルで行う。

```
docs/harness/spec.md
docs/harness/sprints/sprint-NN/{contract.md, contract-review.md, self-review.md, evaluation-R.md}
```

合否判定: 機能性 ≥8、仕様充足度 ≥7、デザイン ≥6、コード品質 ≥6。1つでも閾値を下回るか、契約の完了条件が1つでも未達、または起動失敗・リグレッションがあれば不合格。同じスプリントで5ラウンド不合格になったら、ループを止めてユーザーに判断を仰ぐ。
